# Tools

Scripts for managing the LanceDB Viewer project.

## version-sync.js

Syncs version number in `package.json` and `src-tauri/tauri.conf.json`.

**Usage:**

```bash
npm run version:sync -- 1.0.1
```
